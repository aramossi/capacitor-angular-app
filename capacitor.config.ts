import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.music.app',
  appName: 'capacitor-angular',
  webDir: 'dist/capacitor-angular/browser'
};

export default config;
